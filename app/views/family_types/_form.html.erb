<div class="row">
    <div class="span6 offset3">
      <%= form_for @family_type, :html => {:class => "well"}  do |f| %>
        <% if @family_type.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(@family_type.errors.count, "error") %> prohibited this activity from being saved:</h2>

            <ul>
            <% @family_type.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
            </ul>
          </div>
        <% end %>

       
          <label>Name</label>
          <%= f.text_field :name %>
       
        
          <label>Description</label>
          <%= f.text_area :description, size: "24x6" %>
          
          <labvel>Hours Required to meet fulfillment </label>
          <%= f.text_field :hours_required, size: 3 %>
          
                
        
          <%= f.submit "Submit", class: "btn btn-large btn-primary" %>
        
      <% end %>
    </div>
</div>  
