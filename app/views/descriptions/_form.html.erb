<div class="row">
    <div class="span6 offset3">
      <%= form_for @description, :html => {:class => "well"}  do |f| %>
        <% if @description.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(@description.errors.count, "error") %> prohibited this activity from being saved:</h2>

            <ul>
            <% @description.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
            </ul>
          </div>
        <% end %>

       
          <label>Name</label>
          <%= f.text_field :name %>

        
          <%= f.submit "Submit", class: "btn btn-large btn-primary" %>
        
      <% end %>
    </div>
</div>  
